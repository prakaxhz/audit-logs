# Backend

Express API (MVC-layered) + MongoDB. See the [root README](../README.md) for full architecture notes,
API reference, and technical decisions.

## Quick start

```
npm install
cp .env.example .env   # then set MONGO_URI
npm run dev            # http://localhost:5000
```
