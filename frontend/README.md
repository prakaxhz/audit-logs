# Frontend

React (Vite) dashboard using Redux Toolkit + RTK Query. See the [root README](../README.md) for full
architecture notes and technical decisions.

## Quick start

```
npm install
cp .env.example .env   # VITE_API_BASE_URL, defaults to http://localhost:5000/api
npm run dev            # http://localhost:5173
```
